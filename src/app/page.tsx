import { Button, Typography } from "@/components";

const { H1 } = Typography;

export default function Home() {
  return (
    <main>
      <H1>BADI!</H1>
      <Button>CLICK ME!</Button>
    </main>
  );
}
