import { Typography } from "@/components";
import { cn } from "@/lib/utils";

const { H1 } = Typography;

export default function Home() {
  return (
    <main className={cn()}>
      <H1>CONTENT HERE</H1>
    </main>
  );
}
